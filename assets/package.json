{
  "scripts": {
    "deploy": "cd .. && rm -f _build/esbuild* && mix assets.deploy",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "phoenix": "^1.6.6",
    "phoenix_live_view": "^0.17.14"
  }
}