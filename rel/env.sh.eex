#!/bin/sh

# Released-binary startup needs the Phoenix endpoint to actually listen
# for HTTP requests. `mix phx.server` sets server: true automatically;
# the OTP release does not, so we gate it on PHX_SERVER and the runtime
# config flips `server: true` when this is set.
export PHX_SERVER=true
